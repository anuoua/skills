# @askills/json-explorer-cli

## 0.0.1

### Patch Changes

- Ported into the `admin-skills` monorepo and aligned with the sibling CLI
  packages (`@askills/agent-chat-cli`, `@askills/openapi-explorer-cli`): scoped
  package name, unified `package.json` scripts, shared `tsconfig.json`, and
  `node:test`-based test suite.
